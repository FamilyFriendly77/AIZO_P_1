#include "dataUtility.hpp"

template <typename T> void DataUtility<T>::setArrayLen(int len) {
  arrayLen = len;
}

template <typename T> T *DataUtility<T>::generateArray() {
  for (int i = 0; i < arrayLen; i++) {
  };
}
